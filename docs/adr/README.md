# Architecture Decision Records (ADRs)

Add ADRs for the key choices:
- No database (filesystem storage)
- Token storage approach
- Test stack (Vitest + Playwright)
- Caching/revalidation model

## Template
Use `template.md`.
