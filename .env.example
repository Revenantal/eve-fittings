# Public
NEXT_PUBLIC_APP_URL=http://localhost:3000

# EVE SSO / ESI
EVE_CLIENT_ID=replace-me
EVE_CLIENT_SECRET=replace-me
EVE_CALLBACK_URL=http://localhost:3000/api/auth/callback

# Security (used to encrypt refresh tokens at rest)
TOKEN_ENCRYPTION_KEY=replace-me-32+chars

# Storage
FITS_STORAGE_BACKEND=local
FITS_STORAGE_ROOT=./data
BLOB_READ_WRITE_TOKEN=replace-me
CACHE_STORAGE_ROOT=./.cache/eve-fittings
SESSION_STORAGE_BACKEND=local
FITS_BLOB_PREFIX=fits
SESSION_BLOB_PREFIX=sessions
BLOB_JSON_CACHE_TTL_SECONDS=900
BLOB_JSON_CACHE_MAX_ENTRIES=5000

# Optional
LOG_LEVEL=info
SHIP_TYPE_CACHE_TTL_DAYS=30
ESI_USER_AGENT=eve-fittings/1.0.0 (+https://localhost)
SESSION_TTL_HOURS=168
SYNC_MIN_INTERVAL_SECONDS=300
JANICE_API_KEY=replace-me
